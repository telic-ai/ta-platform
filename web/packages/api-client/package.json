{
  "name": "@ta-platform/api-client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "generate": "openapi-typescript ../../../api/openapi.yaml -o ./src/generated/schema.d.ts",
    "lint": "echo 'no-op: lint not yet configured for @ta-platform/api-client'"
  },
  "devDependencies": {
    "openapi-typescript": "^7.4.1",
    "typescript": "^5.6.3"
  }
}
