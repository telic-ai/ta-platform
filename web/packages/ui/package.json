{
  "name": "@ta-platform/ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no-op: lint not yet configured for @ta-platform/ui'"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
