{
  "name": "@ta-platform/candidate",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no-op: lint not yet configured for @ta-platform/candidate'"
  },
  "dependencies": {
    "@ta-platform/api-client": "workspace:*",
    "@ta-platform/ui": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
